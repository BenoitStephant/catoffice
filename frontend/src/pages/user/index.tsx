import React from 'react';

import Layout from '../../layout';

const User: React.FC = () => {
    return <Layout page={{ name: 'users', title: 'Utilisateurs' }}>Utilisateurs</Layout>;
};

export default User;
