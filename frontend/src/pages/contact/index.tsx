import React from 'react';

import Layout from '../../layout';

const Contact: React.FC = () => {
    return (
        <Layout page={{ name: 'contact', title: 'Contacts' }}>
            <>Contacts</>
        </Layout>
    );
};

export default Contact;
